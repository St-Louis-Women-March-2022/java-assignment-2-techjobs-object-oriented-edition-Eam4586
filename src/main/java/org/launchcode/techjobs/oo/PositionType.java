package org.launchcode.techjobs.oo;

import java.util.Objects;

public class PositionType extends JobField
 {
     public PositionType(String value) {
        super(value);
    }
    //    public Location(String value) {//when a location is created it has to take in a value
     //        super(value);//and how that will be constructed is to check JobField
     //    }

    // TODO: Add a custom toString() method that returns the data stored in 'value'.done

}
